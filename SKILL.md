---
name: prd-digest
version: 0.3.0
description: 对互联网产品需求或 PRD 进行需求准入评审，自动区分小型需求与大中型需求。大中型需求使用 7 个正式维度，小型需求使用 5 个正式维度；证据、关键假设、阻断项与专项评审作为横向机制。默认输出结构化评审摘要，用户明确要求时输出详细报告。默认不评价详细技术架构、像素级 UI 或专业法务结论。
---

# PRD Digest：产品需求准入评审

## 1. 目标

本 Skill 用于判断一项产品需求是否具备进入交互设计、技术方案或研发阶段的基本条件。

它不是 PRD 格式检查器，也不是只给一个总分的自动裁判。它重点回答：

1. 用户问题是否真实、明确；
2. 需求是否具有足够价值和影响；
3. 当前方案是否对症并形成业务闭环；
4. 用户是否容易理解、控制并信任；
5. 当前范围和复杂度是否受控；
6. 是否合理复用已有产品能力，并形成清楚的新增边界；
7. 上线后能否判断成败，失败时能否安全调整。

本 Skill 参考以下方法论的底层原则：

- JTBD：用户、情境、任务、替代方案和期望进展；
- RICE：Reach、Impact 和 Confidence；
- Assumption Mapping：事实、推断、假设和验证优先级；
- MoSCoW：范围、必要性和本期明确不做项；
- HEART：采用、任务成功、留存、参与和满意度指标。

本 Skill 不按照方法论名称机械打分，而是将其重新抽象为产品评审维度和横向控制机制。

## 2. 适用范围

适用于：

- 新产品、新模块和新业务能力；
- 核心流程新增或改版；
- 商业化、增长、会员、内容、交易和 SaaS 需求；
- To B 企业解决方案和内部平台需求；
- 已有功能的小范围优化；
- PRD 初审、复审和立项前评估；
- Agentic Coding 或自动化研发之前的需求准入。

默认不承担：

- 详细技术可行性和架构设计；
- 数据库、服务拆分、性能、容量和技术回滚方案；
- 完整市场规模、定价、财务和商业模式论证；
- 像素级视觉、设计系统和完整 UI 可用性评审；
- 专业法务、安全、隐私和行业合规结论；
- 替代真实用户研究和上线实验；
- 替人作出最终产品决策。

能力边界和专项评审触发规则见 `references/review-boundaries.md`。

## 3. 规则文件

执行评审前读取：

1. `references/demand-classification.md`
2. `references/scoring-rubric.md`
3. `references/evidence-and-gates.md`
4. `references/capability-reuse.md`
5. `references/review-boundaries.md`
6. `references/output-template.md`
7. `references/methodology-map.md`

用户明确要求详细评审、逐项评分、关键假设、MVP、指标或能力复用矩阵时，再读取：

8. `references/output-template-detailed.md`

`examples/` 仅用于校准评分尺度与表达方式。示例不能覆盖正式规则。

## 4. 输入

用户可能提供：

- 完整 PRD；
- 需求背景、目标和功能列表；
- 用户反馈、工单、数据或访谈结果；
- 原型、Figma、页面截图或交互说明；
- 旧版与新版差异；
- 现有产品能力地图、相似模块或历史 PRD；
- 埋点、实验和发布方案；
- 需要复审的修改稿。

最低可评审输入：

- 需求要解决的问题；
- 大致目标用户或使用对象；
- 拟采用的产品方案。

信息不足时：

- 不虚构用户、数据、现有能力或设计细节；
- 继续完成暂定评审；
- 降低评审置信度；
- 将关键缺失标为“材料不足”；
- 说明补充材料会影响哪个判断。

## 5. 输出模式

### 5.1 默认模式：digest

用户只要求“评审、评分、看看需求如何”时，使用 `references/output-template.md`。

默认输出：

- 需求类型；
- 综合评分；
- 评审置信度；
- 总体结论；
- 阻断项数量；
- 一句话判断；
- 大中型需求 7 个维度，或小型需求 5 个维度；
- 关键行动建议；
- 必要时的专项评审触发。

### 5.2 详细模式：detailed

出现以下明确意图时，使用 `references/output-template-detailed.md`：

- 详细评审；
- 展开逐项评分和扣分依据；
- 分析事实、推断和关键假设；
- 输出 MoSCoW、MVP、指标或复用矩阵；
- 生成正式评审报告。

用户指定的输出格式优先于默认模式。

## 6. 核心模型

### 6.1 大中型需求：7 个正式维度

1. 需求成立度；
2. 价值与影响；
3. 方案适配与业务闭环；
4. 体验与信任；
5. 范围与复杂度控制；
6. 能力复用与产品杠杆；
7. 验证与可逆性。

### 6.2 小型需求：5 个正式维度

1. 问题与价值；
2. 方案闭环；
3. 体验与信任；
4. 范围与复用；
5. 验证与可逆性。

### 6.3 横向机制

以下内容不作为并列评分维度，而是作用于所有相关判断：

- E0～E4 证据等级；
- 关键假设与致命假设；
- 评分上限；
- 阻断项和评级上限；
- 材料不足；
- 专项评审触发。

这样可以避免“证据不足”在多个维度中被重复扣分。

## 7. 核心约束

### 7.1 区分文档声称与文档证明

内部必须区分：

- 已知事实；
- 合理推断；
- 未验证假设；
- 未知信息。

不得因为 PRD 使用“用户强烈需要”“预计大幅提升”等确定措辞，就把它当成事实。

### 7.2 不因文档长度判断需求规模

需求规模由影响、流程变化、业务重要性、不确定性、规则复杂度和发布风险决定，不由页数、页面数或文档写法决定。

### 7.3 复用不是越多越好

产品能力复用必须说明：

- 复用什么；
- 为什么适用于新场景；
- 哪些直接复用；
- 哪些扩展复用；
- 哪些必须新增；
- 接受哪些限制；
- 为什么有些已有能力不复用。

不得把技术代码、接口或服务复用当成已经成立的产品结论。

### 7.4 UI 评审有明确边界

可以评价：

- 用户任务流程；
- 明显操作摩擦；
- 用户控制权；
- 自动化透明度；
- 信任风险；
- 主要状态和恢复路径。

没有原型或设计稿时，不评价：

- 视觉层级；
- 字体、颜色、间距；
- 控件精致度；
- 微交互；
- 完整无障碍；
- 像素级 UI 质量。

### 7.5 不制造虚假精确

评分用于结构化比较，不代表数学意义上的真实价值。

每个结论必须有材料依据。材料不足时使用证据上限、置信度和“材料不足”，而不是无依据猜测。

## 8. 执行流程

### Step 1：结构化提取

从输入中提取：

- 需求名称与背景；
- 目标用户和角色；
- 触发情境与用户任务；
- 当前替代方式和痛点；
- 目标结果；
- 产品方案和业务流程；
- 功能范围和明确不做项；
- 数据、访谈和反馈证据；
- 成功指标和基线；
- 业务规则、状态和边界；
- 现有相关产品能力；
- 直接复用、扩展复用和新增边界；
- 发布、灰度、关闭和恢复策略；
- 已知限制和专业风险。

缺失项标记为“未提供”。

### Step 2：需求分类

按照 `references/demand-classification.md`：

1. 对八个分类因素评分；
2. 检查强制升级和降级条件；
3. 判定 `small`、`medium` 或 `large`；
4. 选择对应评分模板；
5. 记录分类依据和分类置信度。

分类不确定时，采用更严格模板。

### Step 3：检查需求逻辑链

内部检查：

`目标用户 → 触发情境 → 用户任务 → 当前障碍 → 产品方案 → 用户行为变化 → 结果指标`

识别缺失、矛盾、无依据跳跃和问题方案错位。

### Step 4：评估现有能力复用

按照 `references/capability-reuse.md`：

1. 识别现有相关能力；
2. 判断新旧用户、任务、规则、状态、权限和风险是否匹配；
3. 区分直接复用、扩展复用、新增能力和明确不复用；
4. 识别复用限制；
5. 判断复用假设失败是否会显著扩大 MVP；
6. 判断新增能力是否具有明确而非想象中的后续杠杆。

没有能力地图或历史材料时，不猜测现有能力，标记材料不足。

### Step 5：识别证据与假设

按照 `references/evidence-and-gates.md`：

- 为相关维度标注 E0～E4；
- 识别关键假设；
- 识别致命假设；
- 应用证据评分上限；
- 形成低成本验证建议。

证据与假设作为横向机制，不单独计算一个维度分数。

### Step 6：按需求类型评分

按照 `references/scoring-rubric.md`：

- 每个维度给 0～5 原始分；
- 按权重换算；
- 应用证据上限；
- 记录判定依据、扣分和建议；
- 计算综合评分。

公式：

`维度加权分 = 原始分 ÷ 5 × 维度权重`

`综合评分 = 所有维度加权分之和`

### Step 7：判定每个维度的结果

结果固定为：

- 通过；
- 有条件通过；
- 不通过；
- 材料不足；
- 不适用。

一般映射：

- 4.0～5.0：通过；
- 3.0～3.9：有条件通过；
- 0～2.9：不通过。

但存在阻断项时，以阻断规则为准；无法可靠判断时使用“材料不足”，而不是强行给出通过或不通过。

### Step 8：检查阻断项和专项评审

按照 `references/evidence-and-gates.md` 和 `references/review-boundaries.md`：

1. 先按综合评分获得基础评级；
2. 再应用阻断项评级上限；
3. 多个阻断项采用最严格上限；
4. 记录专项 UX、商业、技术、法务、安全或合规评审触发；
5. 高总分不能抵消阻断项。

### Step 9：生成输出

默认模式：

- 大中型需求输出 7 行评审表；
- 小型需求输出 5 行评审表；
- 每一行使用 `1. `、`2. `、`3. ` 形式编号；
- 禁止使用带圈数字等装饰性编号；
- 判定依据优先使用“依据、扣分、建议”结构；
- 最后输出 2～5 条关键行动。

详细模式按详细模板展开。

## 9. 评审置信度

评审置信度单独计算，不并入综合评分。

分别对以下三项给 0～5 分：

- 输入完整度 C；
- 证据质量 E；
- 内部一致性 I。

公式：

`评审置信度 = ((0.4 × C) + (0.4 × E) + (0.2 × I)) ÷ 5 × 100`

解释：

- 80～100：材料较充分；
- 60～79：结论基本可用，但仍有重要缺失；
- 40～59：暂定评审；
- 0～39：材料不足，分数主要用于问题定位。

不得把“需求质量低”和“材料不足”混为一谈。

## 10. 语言与格式

- 结论先行；
- 使用产品、业务和用户容易理解的语言；
- 每个判断必须说明原因；
- 建议写清“补什么、为什么、如何补”；
- 不堆砌方法论术语；
- 不输出无依据技术结论；
- 不把未知信息写成事实；
- 默认使用普通阿拉伯数字编号，例如 `1. `；
- 不使用带圈数字、罗马序号或装饰性数字符号。

## 11. 最终自检

输出前检查：

- [ ] 是否完成需求分类；
- [ ] 是否选择正确的 7 维或 5 维模板；
- [ ] 是否区分事实、推断、假设和未知；
- [ ] 是否把证据作为横向机制，而非重复评分项；
- [ ] 是否检查问题到指标的逻辑链；
- [ ] 是否检查现有能力复用和新增边界；
- [ ] 是否区分业务闭环、体验信任和产品可逆性；
- [ ] 是否应用证据上限和阻断评级；
- [ ] 是否标记材料不足与专项评审触发；
- [ ] 是否避免详细技术和像素级 UI 结论；
- [ ] 是否全部使用 `1. `、`2. ` 格式编号；
- [ ] 是否给出具体、可执行的下一步行动。
